cryptography>=42.0
pytest>=8.0

# Optional: enables a native balloon notification for incoming messages
# on Windows (gui.py falls back to bell + title badge without it).
pywin32>=306; sys_platform == "win32"
